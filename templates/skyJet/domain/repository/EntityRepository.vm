package  ${virginPackage}.repository;

import java.math.BigDecimal;

import org.springframework.data.jpa.repository.JpaRepository;

import $package.${metaData.getRealClassName()};
#if($composedKey == true)
    import $package.${metaData.primaryKey.getRealClassName()};
#end
/**
* @author Zathura Code Generator Version 23.10 http://zathuracode.org/
* 
* @generationDate ${localDateTime}
*
* 
*/
public interface  ${metaData.getRealClassName()}Repository extends JpaRepository<${metaData.getRealClassName()}, ${metaData.getPrimaryKey().getType().getSimpleName()}> {

}